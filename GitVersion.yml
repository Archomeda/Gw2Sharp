next-version: 0.6.1
assembly-versioning-format: '{MajorMinorPatch}.{env:APPVEYOR_BUILD_NUMBER ?? 0}'
assembly-file-versioning-format: '{MajorMinorPatch}-{BranchName}-{ShortSha}'
assembly-informational-format: '{MajorMinorPatch}-{BranchName}-{ShortSha}'
mode: ContinuousDelivery
